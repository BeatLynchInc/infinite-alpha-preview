# Infinite Alpha: Feedback Preview

Course content for the Infinite Alpha investing education platform by Beat Lynch.

This is a preview build for collecting feedback. Content is draft and subject to change.

## How to Navigate

Open index.html or visit the GitHub Pages URL. Click any course to view it.

## Deployment

This site is configured for GitHub Pages.

### Option 1: GitHub Actions

This repo includes a Pages workflow at `.github/workflows/deploy.yml`.

1. Push the repo to GitHub on the `main` branch.
2. In GitHub, open `Settings > Pages`.
3. Set `Source` to `GitHub Actions`.
4. Pushes to `main` will deploy the site automatically.

### Option 2: Branch Deploy

If you prefer GitHub's built-in static hosting without Actions:

1. Push the repo to GitHub on the `main` branch.
2. In GitHub, open `Settings > Pages`.
3. Set `Source` to `Deploy from a branch`.
4. Choose branch `main` and folder `/ (root)`.

## Known Gaps

`courses/tier-3/pillar-2-accounting-valuation.html` is truncated before the body content.

`courses/tier-3/pillar-3-market-structure.html` is truncated during the hero section.

## Feedback

Send feedback to [Mike's email or preferred contact -- Zach to fill in].
